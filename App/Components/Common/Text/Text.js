import React from 'react';
import { Text } from 'react-native';

// TODO: Base text, implement theme
const BaseText = objProps => <Text {...objProps}>{objProps.text}</Text>;

export default BaseText;
